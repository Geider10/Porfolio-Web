
<header data-modoLight>
    <nav>
        <a href="#inicio" data-ModoLight>Inicio</a>
        <a href="#proyectos"data-ModoLight>Proyectos</a>
        <a href="#sobre-mi"data-ModoLight>Sobre mí</a>
        <a href="#" id="btnModo"data-ModoLight>☀️</a>
        
    </nav>
</header>
<style>
    header{
        position: fixed;
        z-index: 1;
        display: flex;
        justify-content: center;
        width: 100%;
        padding: 20px 0;
        background: transparent;
        top: 0;
    }
    nav{
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 30px;
        background-color: #b4b4b4c9;
		border: 1px solid #dfdfdfc9;
        padding: 5px 20px;
        border-radius: 10px;
   }
   a{
    background: transparent;
   }

</style>
<script>
    const moon = "🌙",sun ="☀️";
    const listaLight = document.querySelectorAll("[data-modoLight]");
    const btnModo = document.getElementById("btnModo");
    btnModo?.addEventListener("click",(e)=>{
        e.preventDefault();
        if(btnModo.textContent == sun){
           btnModo.textContent = moon;
            modoLight();
        }
       else{
           btnModo.textContent = sun;
           modoDark();
       }
    })
    const modoLight= ()=>{
        listaLight.forEach(element => { element.classList.add("modoLight")});
        btnModo?.textContent == moon;
    }
    const modoDark=()=>{
        listaLight.forEach(element => { element.classList.remove("modoLight")});
        btnModo?.textContent == moon;
    }

    // efecto del header

</script>